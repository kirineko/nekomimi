## 1. 调用记录与搜索基础

- [x] 1.1 从 ResponsesProvider 抽取共用调用记录入口并接回主调用和自动命名；验证 transport 捕获字节、hash、重试、取消、脱敏和历史继续回归测试通过。
- [x] 1.2 扩展搜索配置、用途/协议字段及工具调用关联，兼容旧配置；验证配置读写、独立 endpoint、缺凭证、禁用工具与提示词同步测试。
- [x] 1.3 实现 Messages 搜索适配及有界响应读取、超时、取消；用固定响应验证调用 ID 配对、未知字段、完整/部分/空/失败、恶意 URL、超限和无自动重试。
- [x] 1.4 注册 web_search 并将有界结果、完整响应及 usage 关联到 Journal；验证主模型收到配对工具结果，搜索内部消息不污染 Responses 历史，重开/导入不重新搜索。
- [x] 1.5 在可用凭证环境最小实测搜索 endpoint/model，核对当前官方实现并记录请求脱敏证据、真实来源和 usage；未实测不得勾选，凭证不作为常规 CI 前提。

## 2. 文件修改投影

- [x] 2.1 建立仅接受确认成功 edit/write 的变更投影及会话分页查询；验证多轮同文件、失败、prepared 后中断、旧记录缺字段和跨页关联测试。
- [x] 2.2 由保存的 patch 生成有界 hunk、行号和准确统计；验证新建、覆盖、空文件、无变化、CRLF、无末尾换行、大文件、损坏/脱敏证据及磁盘后续变化。
- [x] 2.3 将 diff 投影接入 HTML 导出和 bundle 展示兼容路径；验证离线读取保留原始证据且不读取工作区补齐内容，不触发文件操作。

## 3. 工作区文件服务

- [x] 3.1 增加受认证保护的目录分页接口、排序和隐藏项过滤；验证根目录、大目录、游标失效、目录消失、权限失败及取消请求。
- [x] 3.2 实现规范路径和文件类型检查，并覆盖越界路径、内部/外部 symlink、特殊文件、未认证与错误 Origin；验证拒绝时不读取目标或启动进程。
- [x] 3.3 实现默认程序打开平台适配器和 POST 接口，避免 shell 插值；验证中文、空格、引号及命令元字符路径、文件消失、启动失败、连续请求去重及无自动重试。
- [ ] 3.4 分别完成 macOS、Linux、Windows 的原生打开验收并记录环境及结果；mock 测试与实际桌面打开分列，无对应环境时保留本项未完成及具体缺口。

## 4. 整体视觉与工作台骨架

- [x] 4.1 落实 design.md 的颜色、排版、间距、圆角和组件状态，清理样式覆盖；以空态、长回答、工具和失败样例做视觉复核并记录截图。
- [x] 4.2 重整 Sidebar、顶部栏、消息和工具层级，统一设置、导出、删除、诊断表面；验证既有会话选择、设置保存、删除保护、诊断及导出浏览器流程可用。
- [x] 4.3 实现单一容器聚焦及自动增高 Composer；验证 Enter/Shift+Enter、中文候选确认、提交去重、失败保留草稿、长文本滚动和发送/停止状态。
- [x] 4.4 集成文件/变更/执行详情右侧标签页，将 Inspector 内容迁入统一容器；验证开关、切页、切会话、旧请求取消、阅读位置和草稿不受影响。
- [x] 4.5 实现响应式抽屉和键盘焦点管理；在四个约定视口验证无整体横向溢出、Escape/焦点恢复、文件树键盘操作及主要动作可到达。

## 5. 新能力的可读界面

- [x] 5.1 实现 Diff 卡片、上下文/行号、折叠、复制和完整查看，并接入变更页；浏览器验证新增/覆盖/多次修改/无变化/证据缺失，统计与持久化 patch 一致。
- [x] 5.2 实现按需文件树、隐藏项、分页、刷新、单击选择与双击/键盘打开；验证新增文件自动刷新、从 diff 定位及当前文件消失反馈，不误触发打开。
- [x] 5.3 实现搜索来源卡、状态和完整证据导航，检查器按用途/协议展示搜索调用；验证无结果、部分结果、失败、取消及未知 usage 不伪装成功或零值。
- [x] 5.4 在设置中接入搜索启用、模型和高级 endpoint；验证保存后下一任务生效、运行中请求不变、界面和日志不泄露凭证。

## 6. 集成与验收

- [x] 6.1 完成“提交任务—搜索—edit/write—查看 diff—文件树定位—提交默认程序打开—刷新历史—导出”的浏览器闭环；验证回放零副作用、历史分页及切会话不串数据。
- [x] 6.2 在 1920×1080、1440×900、1280×720、390×844 复核完整视觉清单，记录截图、文字对比度及键盘/中文输入法结果；发现遮挡或焦点问题后复测。
- [x] 6.3 运行 npm run typecheck、npm test、npm run test:browser、npm run test:pack 和 openspec validate --all --strict；记录命令结果，检查安装包包含新 UI 与服务模块。
- [x] 6.4 更新 README、使用说明和实施验证记录，明确逐次 diff 范围、本机打开语义、搜索配置及平台实测缺口；核对说明与最终实现一致，未验证任务保持未勾选。

## Validation Notes

本机实现与验证已完成，证据见 `docs/validation/workbench-upgrade/README.md`。3.4 已完成 macOS 原生打开请求验收；缺少 Linux/Windows 桌面环境，两者不以模拟结果代替，故该项保持未完成。用户在知晓该缺口后于 2026-09-15 明确要求归档；本项保持未完成，不代表三平台验收已通过。

## 7. Review 修复

- [x] 7.1 保留文件树逻辑路径并阻止祖先符号链接循环；验证内部别名仍可浏览和打开、循环条目不可展开及直接循环请求拒绝。
- [x] 7.2 变更面板增量合并记录并使用稳定序号分页；验证连续 Journal 更新、超过一页的新修改、历史分页、展开 diff 保留和切会话清理，再次 review。
