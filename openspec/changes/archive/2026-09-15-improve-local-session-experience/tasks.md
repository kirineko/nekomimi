## 1. 配置与统一存储

- [x] 1.1 新增独立 config/storage 模块，统一用户根目录、工作区规范化及数据分区；验证 CLI/Web 同工作区一致、其他工作区隔离及显式测试 home 不访问真实用户目录。
- [x] 1.2 实现版本化 settings/auth、原子写入、修订冲突和权限处理；验证损坏配置、并发保存、写入失败、密钥替换与清除不破坏旧文件。
- [x] 1.3 将 CLI/Web 产品启动切换到文件配置，新增 CLI 交互配置与 --home，接受运行时捕获配置快照；验证仅环境变量中有 key 时仍拒绝提交、运行中更新只影响新任务及凭证不回显。
- [x] 1.4 新增已授权设置接口及独立 Web 设置组件，提供模型、地址、凭证状态与保存/清除；浏览器验证首次设置→提交→重启仍可用，以及跨站写拒绝、保存失败反馈和窄屏操作。

## 2. 旧会话迁移

- [x] 2.1 实现已知旧目录的只读迁移预览、工作区归属和活动锁检查；验证范围、symlink、旧 CLI 会话及错误路径不泄露其他工作区历史。
- [x] 2.2 实现复制、日志与附件校验、原子发布和迁移记录；故障注入验证中断可重试、冲突不覆盖、重复迁移跳过、源字节不变和零模型/工具调用。
- [x] 2.3 接入 CLI 显式迁移入口（按最新反馈移除 Web 入口）；验证损坏会话逐项报错、未完成历史保留未知状态且不自动继续，迁移后 CLI/Web 可读同一会话。

## 3. 会话命名与生命周期

- [x] 3.1 新增标题事件及列表投影，第一条消息生成临时标题，兼容旧 web.session；验证刷新、多窗口和重建后的标题一致，不改写旧日志。
- [x] 3.2 通过统一 provider 实现首轮辅助命名，标注用途、限定输入/输出和超时；验证一次命名、合法输出校验、失败回退、无工具、证据完整及辅助输出不进入任务上下文。
- [x] 3.3 将命名纳入单写入调度，用户继续优先取消辅助调用；验证任务竞争、取消落盘后交接、崩溃不自动重发以及主任务状态不受命名失败影响。
- [x] 3.4 实现会话确认删除接口、生命周期门禁、隔离后清理与幂等处理；验证活动/命名 busy、并发提交和导出保护、symlink 拒绝、失败恢复且不删除工作区文件或复活已迁移会话。
- [x] 3.5 增加侧栏删除菜单和 deleted 通知处理；浏览器验证确认/取消、当前会话删除后空白状态、其他窗口停止重连及重启列表不恢复。

## 4. 输入交互

- [x] 4.1 实现 Enter 发送、Shift+Enter 换行与输入法组合保护，沿用提交去重；浏览器验证中文选词、组合结束 Enter、长按、忙碌、失败保留草稿及按钮提交一致性，并记录真实输入法人工验收结果。

## 5. 离线 HTML 阅读

- [x] 5.1 提取纯展示模型及工具配对规则，分离 export 模板/样式与 bundle 逻辑；验证相同 Journal 的 Web/离线消息顺序、辅助调用隔离和工具结果配对，不改变请求构造。
- [x] 5.2 实现 Nekomimi 标题、轮次目录、Markdown、工具结果/diff、原始证据锚点和折叠层级；离线浏览器验证无服务可读、多轮定位及桌面/窄屏布局，截图只输出到忽略目录。
- [x] 5.3 保持渲染前脱敏、CSP、无外部资源及未完成状态；验证恶意 HTML/链接/图片、原始附件、未知结果、缺失 usage 与源 Journal 不变，回归 bundle 导出/导入兼容。

## 6. 集成与交付

- [x] 6.1 更新 README、源码模块文档、CLI 帮助、真实冒烟脚本和测试配置为文件凭证；检查运行入口不再依赖产品环境变量，所有测试在临时 home 中运行。
- [x] 6.2 完成配置→创建→Enter 提交→自动命名→继续→HTML 导出→确认删除的浏览器集成；显式使用用户配置执行一次最小真实模型/命名验收，无凭证时明确记录未验证并保留任务未完成。
- [x] 6.3 运行 typecheck、全量测试、浏览器回归、npm pack 干净安装及 openspec validate --all --strict；记录平台限制与验收结果，全部通过后再勾选并交付，归档另行执行。

## 7. 产品重命名

- [x] 7.1 将当前产品品牌、目标包/仓库名和 bin 统一为 Nekomimi/nekomimi，更新代码、帮助、锁文件、文档、脚本和测试；检索旧名逐项确认仅历史证据、协议兼容与迁移路径保留，归档规格不改写。
- [x] 7.2 验证 npm pack 干净安装后 `nekomimi --help`、`nekomimi web` 和配置入口可用，默认目录为 `.nekomimi`，旧 bundle 可导入且日志 hash 不变；记录发布前 npm 名称核查尚待完成，不执行发布或远端重命名。

## 8. Review 修复

- [x] 8.1 HTML 脱敏保留展示结构和附件引用，回归字段名碰撞。
- [x] 8.2 迁移登记事务互斥，CLI 遵守工作区租约，回归并发迁移及删除后不复活。
