> 2026-09-22 用户调整：本轮暂不验证 Linux、Windows。交付 macOS / Node 22.19.0、24.15.0 的本地候选；10.1、10.4 的跨平台部分及依赖它们的最终归档保留待办，不将延期项勾选为通过。公开发布仍遵循原 CI 门禁。

## 1. 基线与公共契约（第一批）

- [x] 1.1 复验 V1 并同步/归档 `enable-user-customization-mvp`，保留已有实现和用户修改；运行 V1 相关回归及 `openspec validate --all --strict`，核对六份 V1 新主规格与两份修改没有丢失场景。
- [x] 1.2 定义 SDK 2、包清单/锁、Provider、workflow、UI 和 RPC 版本契约及 SDK 1 映射；以类型 fixture 验证未知能力拒绝、V1 公共 API 编译、稳定资源身份与状态命名空间。
- [x] 1.3 固定内容/消息/队列/进程预算及新增 Journal 关联字段；用超限、慢消费者、缺失 artifact 和旧事件 fixture 验证有界错误、证据缺口与旧历史读取。
- [x] 1.4 核验并锁定受控 UI 打包器、MCP OAuth 公开接口及现有 pi 接入；用 Node 22.19.0/24.15.0 的最小编译、JSON/SSE、PKCE/refresh 探针记录具体版本和协议支持矩阵，失败不得用占位实现替代。

## 2. 独立扩展进程与宿主服务（第一批）

- [x] 2.1 实现子进程启动、握手、注册描述和 handler 调用，将工厂执行移出主进程；验证 V1 工具/命令/钩子/状态/UI 示例行为及未知 SDK、重复注册拒绝。
- [x] 2.2 实现带 instance/resource/revision/run/call 身份的 RPC 与宿主服务授权；验证伪造身份、过期消息、越权工具/目标被拒绝，副作用前意图已落盘。
- [x] 2.3 实现 ACK/背压、帧及队列上限、stdout/stderr 分离；用大结果、日志洪泛和慢磁盘 fixture 验证内存/队列有界及模型投影与原始证据分离。
- [x] 2.4 实现取消、超时、进程树退出确认和强制清理；本轮在 macOS 用死循环、拒绝退出、执行后崩溃和迟到响应 fixture 验证宿主可响应、unknown、不自动重发及 degraded。
- [x] 2.5 实现候选进程注册、原子切换和旧 revision 引用保留；验证重载失败保留旧版本、运行中重载不死锁、重复切换不重复监听、撤权阻止新的宿主调用。

## 3. 对话开发与候选版本（第一批）

- [x] 3.1 增加模型可读 SDK 目录/签名查询、脚手架与候选管理工具及 CLI；在无源码 checkout 的 fixture 中生成并定位候选，不写安装目录。
- [x] 3.2 实现清单/依赖及完整 TypeScript 检查，结构化诊断绑定内容 hash；验证语法正确但类型错误、缺失依赖、SDK 不兼容及检查后改写均不能误通过，静态检查不执行代码。
- [x] 3.3 实现独立候选试验、可替换模拟服务和显式真实使用入口；验证模拟结果有标记、真实工具有完整 Journal、取消试验不改变活动注册，可信 Node 边界明确。
- [x] 3.4 实现不可变候选内容存储、活动指针/锁提交和回退；逐个提交窗口注入崩溃，验证恢复无半激活、代码回退不删除历史且状态不兼容时停止。
- [x] 3.5 在 Web 展示候选检查、内容/权限差异、预览、激活收据和回退；浏览器验证失败位置可定位、旧版本持续可用及新增权限待授权。

## 4. 能力包分发与 Skills/Rules 组合（第一批）

- [x] 4.1 实现能力包资源清单、项目/用户安装范围及包内独立身份；fixture 验证 V1 裸资源兼容、项目覆盖、同层冲突、Rule 目标绑定和 Skill/MCP prompt 不混淆。
- [x] 4.2 实现本地目录与 npm 来源的获取、独立依赖缓存和精确锁定；本地 registry fixture 验证 integrity、浮动 tag 更新差异、取消回滚且不执行 lifecycle scripts、不修改宿主依赖。
- [x] 4.3 实现固定 Git commit 来源和安全内容导入；临时 Git 仓库及恶意归档 fixture 验证 branch 固定、hooks/submodule 不执行、路径/链接/体积越界拒绝。
- [x] 4.4 实现包受控构建与候选事务安装/更新；验证构建不自动加载用户可执行配置，缺依赖/网络中断/构建失败保留原锁及活动版本，授权扩展不静默继承。
- [x] 4.5 实现包分享导出/导入、卸载和内容引用清理；第二工作区重装验证依赖可复现、规则重新绑定、凭证/私有状态不打包、挂起版本不误删且历史可读。
- [x] 4.6 完成包管理 CLI/Web 入口及第一批综合验收；对话生成带 Skill/Rules 的命令和工具，完成校验修复、激活、升级、回退和分享，以实际调用及 revision 证据验证。

## 5. Provider 与模型配置（第二批）

- [x] 5.1 建立 Provider/model 注册与能力目录，迁移默认 DeepSeek adapter；运行原 provider/context/search 回归并验证重复身份、未知能力和注册失败不覆盖默认模型。
- [x] 5.2 实现自定义 adapter 的序列化/解析 RPC 与宿主 RecordedCall transport；fixture 比对实际请求字节，验证逐 attempt、原始 JSON/SSE、背压、parser 异常和取消后不调度工具。
- [x] 5.3 实现 provider profiles、服务端凭证引用和旧 settings/auth 迁移；验证原配置兼容、迁移备份、失败回退、未授权端点/重定向不转发密钥及日志/包导出脱敏。
- [x] 5.4 接入主对话、辅助模型与自动命名的模型选择及能力检查；验证运行版本固定、不支持图片/工具提前拒绝、辅助上下文独立，内置搜索不随主模型隐式换协议。
- [x] 5.5 实现历史兼容检查和显式跨 Provider 分支；含 reasoning、图片及多工具历史的 fixture 验证来源保留、call ID 配对、不伪造历史及不兼容诊断。
- [x] 5.6 交付 Responses/Chat Completions adapter 示例和模型管理 Web；本地服务及浏览器验证对话创建 adapter、启用选择、实际工具回合和请求证据，无真实账户前提。

## 6. MCP 内容与授权（第二批）

- [x] 6.1 实现 resources/templates 分页目录、参数化读取及原始内容保存；fixture 验证同名隔离、重复游标、MIME/图片/二进制投影、链接不自动读取与超限诊断。
- [x] 6.2 实现 prompts 目录、参数校验、显式获取及上下文注入策略；验证原始消息角色/参数可追溯、恶意提示不提升权限、停用撤销与历史保留。
- [x] 6.3 实现资源订阅/退订、通知合并、断线和版本提示；fixture 验证活动请求/步骤快照不变、重新读取才刷新、断线不重发工具副作用。
- [x] 6.4 实现 HTTP OAuth 发现、客户端配置、PKCE、回环回调和服务/issuer/资源绑定；本地授权服务验证正常授权、错误 state、重放、过期、恶意端点与不支持流程拒绝。
- [x] 6.5 实现 OAuth 服务端持久凭证、刷新、断开/重新授权及 CLI/Web 状态；重启、并发刷新和失效 fixture 验证状态一致，token 不进入 UI、日志、模型或导出。
- [x] 6.6 完成第二批 Provider + MCP + Skills/Rules 综合验收；通过本地协议服务检查最终请求的内容来源/角色/版本及取消、认证失败和服务器内容更新边界。

## 7. 持久工作流与状态（第三批）

- [x] 7.1 定义并实现工作流注册、输入 schema、步骤身份、转移及独立 Journal；验证固定 definition revision、合法/非法转移、旧会话通过引用展示且无双重权威结果。
- [x] 7.2 实现步骤意图/完成、检查点及显式恢复；各崩溃窗口验证已完成步骤不重跑，未开始步骤可继续，执行后无结果标为 unknown。
- [x] 7.3 实现 unknown 核对、提交外部结果或明确授权新 attempt 的入口；验证没有自动重试/隐式补偿、选择持久化、重复请求不重复推进。
- [x] 7.4 实现持久交互等待、跨重启回答和取消；服务/browser 测试验证稳定交互身份、多窗口冲突、旧回答拒绝及 V1 临时表单保持原语义。
- [x] 7.5 增加工作区级状态 Journal、schema 迁移和跨 Journal outbox 去重；注入并发写与投递前后崩溃，验证 CAS、补投不重执行及 V1 会话状态兼容。
- [x] 7.6 实现命令/运行完成/工具完成事件触发、有界队列和因果预算；验证单活动运行、重复事件、循环触发、挂起释放占用和重启不自动执行副作用。
- [x] 7.7 实现挂起流程版本保留、撤权/卸载阻断和显式迁移；验证升级不偷换定义、旧 revision 不误清理、失败迁移保留原状态并可取消。
- [x] 7.8 提供工作流 CLI/Web 状态、恢复与证据查看；端到端验证审查暂停、关服务、重启回答、完成及 unknown 人工处置闭环。

## 8. 自定义 Web 面板与主题（第三批）

- [x] 8.1 实现 panel/renderer 清单、SDK 类型和受控静态构建；tarball fixture 验证无需源码生成组件、类型错误诊断、产物 hash 与 revision 匹配及 V1 UI 兼容。
- [x] 8.2 实现 sandbox iframe、独立 CSP、实例握手与 MessageChannel；真实浏览器验证宿主 DOM/cookie 隔离、伪造 source/nonce、未授权网络/导航和超限消息被拒绝。
- [x] 8.3 实现受控宿主动作、工作流交互桥接和权限重验；浏览器验证组件重建/刷新/多窗口重发不重复执行、取消后旧面板不推进流程。
- [x] 8.4 实现预览、插槽、主题 token 校验与崩溃回退；浏览器验证筛选/确认面板、主题切换、组件崩溃后仍可取消且授权界面保持可见。
- [x] 8.5 实现历史结构化 UI 数据、文本 fallback 与导入/离线导出；删除包和资源后验证仍可读、缺失 artifact 明确报错，离线无脚本执行或外部下载。

## 9. 原始需求组合验收与真实演练

- [x] 9.1 扩充随包 SDK 文档、脚手架和八类资源示例，更新 README、开发/配置/迁移说明与 spec.md 能力状态；全部示例通过类型、注册和实际安装后使用检查，不把 OS 沙箱或未实测供应商描述为已支持。
- [x] 9.2 构建确定性模型生成的完整 /review 能力包，读取合成 Git diff，组合工具、Skill、目标 Rules、MCP resources/prompts/OAuth、自定义 Provider、持久步骤和面板；检查生成/修复/激活/实际调用的完整证据链。
- [x] 9.3 浏览器与第二工作区验证暂停重启、回答、包升级/回退、导出分享/重装、撤权/卸载及无插件历史；逐项核对 design 的原始需求矩阵，任一未通过不宣告 V2 完成。
- [x] 9.4 在本轮 macOS 范围完成故障矩阵：进程死循环、磁盘失败、依赖篡改、更新中断、跨 Journal 崩溃、MCP 断线、OAuth 失效、parser 异常、UI 越权和状态不兼容；检查无未知副作用自动重放、无凭证泄露及有界资源使用。
- [x] 9.5 单独执行真实模型对话演练，覆盖命令、工具、Provider、工作流和 UI 的生成、修复、实际使用；记录模型标识、attempt、耗时、自然/注入失败及所有模拟服务边界，凭证不加入 CI。

## 10. 版本候选交付

- [ ] 10.1 补齐 Linux/macOS × Node 22.19.0/24.15.0 及 Windows 最低 Node 的安装、进程/MCP 清理、Web 冒烟验收入口；取得真实平台结果，无法取得的项保持未完成，不以 fixture 平台模拟替代原生证据。
- [x] 10.2 核查候选版本（建议 0.2.0）未被官方 registry 占用，更新 package/lock、SDK 资料版本和发布/迁移说明；运行元数据/清单检查，查询显式使用官方 registry。
- [x] 10.3 运行 `npm run typecheck`、`npm test`、`npm run test:browser`、`npm run test:release`、`npm run test:pack` 和 `openspec validate --all --strict`；验证所有必需场景通过，保留结果及 V1 兼容对照。
- [ ] 10.4 从确定的候选内容构建最终 tarball，核验同一包在支持平台的本地/全局安装、完整能力包闭环及迁移；记录版本、integrity、源码标识、平台与账号独立 fixture 结果，发布包不含开发凭证或会话。
- [ ] 10.5 汇总十一份规格场景映射、真实演练与发布候选报告，在必需门槛全部通过后同步/归档 V2；交付 tarball、迁移和发布说明，明确是否仅为候选。公开 push/tag/Release/npm 上传须另按用户发版指令和既有 CI 门禁执行。

## 归档与发版衔接（2026-09-22）

用户在知悉 52/55 及延期平台验收后明确要求归档并发布 0.2.0。本次按该授权同步规格并归档，保留以上三项未完成标记作为归档时的真实状态；其跨平台和发布部分转由本次标签 CI 与发布核验完成，结果记录在 `docs/releases/0.2.0.md`，不追溯伪造归档前已通过。
