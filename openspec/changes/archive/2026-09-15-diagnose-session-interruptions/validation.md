# 验证记录

## 已完成
- 类型检查通过；79 项单元/集成测试、6 项 Chrome 浏览器测试、7 项发布检查通过。
- OpenSpec 严格校验 14 项通过。
- 对 watermark open/write/sync/rename/directory-sync 定点注入 EPERM，诊断阶段、错误码、seq/durableSeq 正确，日志保持停止执行，不生成伪造结束记录。
- 诊断目录不可写模拟：原始诊断和回退提示出现在 stderr，原始错误不被覆盖；含测试密钥的原始异常正文未进入诊断。
- 任务接受后落盘错误可通过 API 获取，重启服务后仍可读取；页面在游标和状态均不变时收到诊断并可下载。
- 取消回归确认不产生诊断；旧会话无需迁移。
- 全局安装包、CLI、Web 资源、文件配置、工具执行、工作区隔离及导出导入检查通过。
- React 检查：按需下载、不增加轮询、不引入 Node 客户端依赖；原生 details/summary 可键盘操作，故障标识变化时重置组件局部状态。

## 现场限制
未在原生 Windows 重新触发现场故障。现有故障注入证明该代码路径的行为，不证明现场根因。任务 2.3 保留未完成；不发正式 npm 版本、不加入猜测性重试。

## 预览包
以已通过安装验收的 tarball 提供本地诊断预览包，包内版本仍为 0.1.2，不是官方 npm 的同版本包。SHA256：`90242de4cd0a3c84c2ee3dd3eca8d40831c743754bdb9f369d5f02920635ca69`。

采集步骤见 `docs/runtime-diagnostics.md`，普通会话导出与诊断下载需分别提供。

## Review 修复
- 诊断下载绕过 JournalReader 和 durable.json，通过路径边界及首条 session.created 的哈希/工作区校验保留身份约束；首条身份记录也不可读时仍拒绝访问。
- Entry 初始化先注册共享对象，再由同一串行队列加载诊断，避免并发创建不同缓存实例。
- 新增损坏 watermark 下重启后下载、无授权拒绝、跨工作区身份拒绝，以及 8 个并发首次读取共享同一 Entry 的回归。
- 21 项相关单元/服务测试、6 项浏览器测试、类型检查、14 项 OpenSpec 严格校验通过；再次 review 未发现新的可操作问题。
- 预览包已重新生成；此前安装闭环验收针对首次诊断实现，本轮修复使用上述定向回归验证。

## 归档决定
用户要求归档后本地提交，暂不 push。已完成诊断增强和 review 修复；任务 2.3 的 Windows 现场采集及根因修复仍未完成，按用户要求保留未勾选并随本次归档记录，后续收到新证据再继续。临时预览包已按用户要求清理，本文哈希仅为历史验收记录。
