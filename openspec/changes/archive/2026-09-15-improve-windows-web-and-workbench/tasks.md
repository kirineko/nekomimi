## 1. Windows 修复

- [x] 1.1 提取跨平台静态资源边界判断并修复工作区名称；以 Windows/POSIX、中文空格路径、兄弟目录、跨盘及 symlink 越界回归验证。
- [ ] 1.2 修复全局安装脚本的 npm shim、工具名与 shell 假设；在原生 Windows 验证 CLI、静态资源、配置、文件操作及两个工作区隔离。
- [x] 1.3 增加标签 CI 的 Windows 验收任务并同步发布流程文档；解析工作流确认普通 push/PR 不触发，记录原生结果后才更新支持声明。

## 2. 视觉设计与实现

- [x] 2.1 按 frontend-app-builder 生成完整会话、追踪展开及窄屏设计概念；用户确认后记录字体、间距、容器和组件规范，图片仅放临时目录。
- [x] 2.2 基于确认设计模块化调整侧栏、顶栏、消息与输入区域，保留 Brand；四种目标视口对照设计确认比例、对齐、阅读宽度和无遮挡。
- [x] 2.3 实现纯展示层轮次分组和状态收拢；用正常/失败/取消/未知/命名/分页边界样例验证证据可达且 Journal 不变。
- [x] 2.4 优化追踪并排与抽屉布局，验证键盘焦点、关闭恢复、独立代码滚动和窄屏操作。

## 3. 回归与交付

- [x] 3.1 使用 react-best-practices 检查修改的 TSX、Effect 清理、稳定 key 和渲染边界；记录长会话流式输入与滚动测量，只实施有依据的性能调整。
- [x] 3.2 执行类型、单元、浏览器、发布及安装包检查；回归 IME、发送、取消、删除、命名、重连、分页和导出，确认失败/未知状态未隐藏。
- [ ] 3.3 对照确认设计完成四种视口视觉验收，记录原生 Windows 与其他平台结果及限制；运行 openspec validate --all --strict，未验证项不得勾选。
