## 1. 工程与协议基础

- [x] 1.1 核对 React/Vite 官方 Node 支持范围并锁定依赖，建立 Web 构建与类型边界；验证干净安装、现有 Node 构建和浏览器构建通过。
- [x] 1.2 定义版本化会话、命令、快照、事件游标及错误协议；通过 schema/fixture 验证错误输入、旧 runId 与未知版本处理。
- [x] 1.3 增加固定工作区管理目录和单服务租约，建立可重建会话列表；验证服务重启、symlink 逃逸、跨工作区访问和第二服务冲突。

## 2. 服务端命令闭环

- [x] 2.1 接入 run 的持锁初始化和 command.accepted 持久化确认，记录 commandId/负载 hash/runId；测试重复提交、负载冲突和确认响应丢失不会重复调用模型。
- [x] 2.2 实现创建、列表、提交、取消与单活动任务限制；验证其他会话可查看、繁忙返回冲突、旧运行取消不会停止新运行。
- [x] 2.3 实现运行生命周期和重启恢复；用强制终止测试确认 accepted 未完成命令显示 interrupted/unknown，重发原命令不自动执行，显式新提交才继续。
- [x] 2.4 接入本地令牌、Host/Origin 校验和会话授权；测试跨来源、错误令牌、任意路径/附件越界及无 API key 时只读可用且提交未接受。
- [x] 2.5 新增 web CLI 入口与优雅停止；验证 loopback 绑定、启动入口、服务退出等待取消终态，以及既有 CLI 命令回归。

## 3. 增量历史与展示投影

- [x] 3.1 实现带水位的快照和有界增量读取；使用快照/订阅交错 fixture 验证无漏事件，避免逐事件重读全量 Journal。
- [x] 3.2 实现带游标的 SSE 和慢客户端缓冲上限；测试断线补读、重复去重、非法游标 reset、恢复截断，以及慢客户端不阻塞运行。
- [x] 3.3 从原始证据生成文本、工具和调用摘要投影；覆盖 SSE 跨 chunk、多 attempt、unknown、failed/incomplete、截断和重建一致性。

## 4. Web 任务工作台

- [x] 4.1 实现会话列表、新建/切换、多行输入及提交确认；浏览器测试验证创建到完成再继续的历史链路和双击去重。
- [x] 4.2 实现增量时间线、连接状态和运行/取消状态；浏览器测试刷新、断线和双窗口不会重启或自动取消任务。
- [x] 4.3 实现工具参数/结果卡片、文件 diff、shell 双通道和附件续读入口；使用长输出、失败与文件修改 fixture 验证展示正确。
- [x] 4.4 实现时间线窗口化、会话分页、窄窗口抽屉与键盘操作；用长日志、窄窗口及恶意 HTML fixture 验证可用性和内容隔离。

## 5. 证据检查与下载

- [x] 5.1 实现调用/attempt 检查面板及 Prompt/Context/Request 链接；浏览器验收从最终 body 定位输入来源、配对工具结果和完整附件。
- [x] 5.2 实现原始响应、usage/缓存/首字节/总延迟显示；测试重试、unknown 事件、缺失字段和无价格数据不会误报费用。
- [x] 5.3 实现会话限定的有界附件接口和错误视图；测试 hash 损坏、缺失文件、非法附件引用及分页边界。
- [x] 5.4 实现终态会话 HTML/完整 bundle/显式脱敏 bundle 下载；验证运行中导出提示、下载授权、源记录不变、无模型/工具调用及 CLI 校验导出包。

## 6. 整体交付验收

- [x] 6.1 完成浏览器端到端闭环：新建、提交、真实工具 fixture、观察 diff、取消、继续、请求溯源、导出；记录四项新增 capability 的场景结果。
- [x] 6.2 在显式凭证可用时运行受限合成任务的真实浏览器→服务→DeepSeek→工具验收；无凭证明确标记未运行，不以旧 headless 结果替代。
- [x] 6.3 将静态资源纳入 npm 单包，在干净目录安装 tarball 后启动 Web 并验证页面、任务 fixture 和离线下载；记录实际浏览器/操作系统覆盖，不发布。
- [x] 6.4 更新启动与使用文档、协议限制和验收报告；运行类型检查、构建、原有测试、新增协议/浏览器测试及 OpenSpec 严格校验，保持前序 change 未被隐式归档。

## 验收记录

44 项自动化测试、3 项浏览器测试及真实模型链路均通过。详见 `docs/web-mvp-validation.md`，模块边界见 `src/README.md`。当前 change 保留待审阅，未归档。
