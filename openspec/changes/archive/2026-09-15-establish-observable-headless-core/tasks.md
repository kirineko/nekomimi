## 1. 将已验证依赖接入应用工程

- [x] 1.1 建立 Node LTS/TypeScript strict/ESM、Vitest 和构建配置；验证 typecheck、空工程构建及基础测试入口成功。
- [x] 1.2 实现 pi 低层 Agent 运行时边界，将 transcript 作为 Journal 可重建投影；用验证报告 V02/V03 的异步门禁和失败阻断场景验证应用接入。
- [x] 1.3 在应用中固定 pi-agent-core/pi-ai 0.85.1 和 lockfile；验证干净安装与公开入口导入成功。

## 2. Journal 与附件

- [x] 2.1 实现版本化事件及单会话写入器；以并发事件测试验证 seq 唯一递增、关联 ID 可重建。
- [x] 2.2 实现内容寻址附件和完整性检查；验证大输出、缺失附件、hash 不匹配及引用前落盘顺序。
- [x] 2.3 实现 dispatch/commit fsync、流批量和水位；故障注入验证日志失败后无新副作用，实测并记录刷盘延迟。
- [x] 2.4 实现恢复与损坏检测；强制终止验证尾行隔离、中段损坏报错、unknown 操作不自动重跑。

## 3. 模型与上下文

- [x] 3.1 实现提示词贡献和有序 ContextView manifest；验证工具禁用同步撤销指导、稳定组装及逐项来源追溯。
- [x] 3.2 实现基于 pi-ai 公开 stream/onPayload/fetch 的 Responses adapter 与冻结请求证据；复用 V05/V07 夹具验证 body/hash、instructions/system 语义、无重复系统指令及凭证排除。
- [x] 3.3 实现原始 SSE、终态、重试与取消记录；覆盖未知事件、reasoning、多工具、限流、failed/incomplete、断流及取消后的零新增请求，确认内层 maxRetries:0、外层每次尝试独立记账。
- [x] 3.4 接入选定 pi 循环与原始 item 历史投影；验证 call/result 与原始 reasoning 配对、thinking 使用 auto、显式继续、用户/工具图片输入，以及失败响应不触发工具执行。

- [x] 3.5 实现响应字节/事件数量上限与 Journal 流写入背压；用慢盘、慢显示订阅和超限流验证内存有界及中止证据，不静默丢弃原始事件。

## 4. 基础工具

- [x] 4.1 实现 read 及有界模型结果；验证文本/图片、offset/limit、长内容续读和完整附件证据。
- [x] 4.2 实现自有精确 edit/write 和路径边界、新鲜度检查；验证与 V14 不同的模糊匹配拒绝、重复/重叠匹配、外部修改、symlink 逃逸、编码换行及无部分写入。
- [x] 4.3 实现实际 shell 选择、双通道输出与取消；验证长输出、退出码、超时、进程停止前不宣告取消完成。
- [x] 4.4 串联意图、执行证据、展示与模型结果；端到端验证任意工具调用可追溯实际参数及完整结果。

## 5. Headless 与导出

- [x] 5.1 实现 run/resume、结构化输出、SIGINT 和单会话写入互斥；验证重复并发提交冲突及取消终态。
- [x] 5.2 实现只读回放与诊断包导入检查；使用模型/工具调用计数器验证回放及导入均零副作用。
- [x] 5.3 实现自包含 HTML 和诊断 bundle；验证附件 manifest、恶意 HTML 转义、无外网请求、脱敏标记及源记录不变。

## 6. 集成验收

- [x] 6.1 完成请求→工具→后续请求→取消/故障恢复→离线导出集成测试；逐项对照五个 capability 的场景给出结果。
- [x] 6.2 增加凭证存在时显式运行的真实 DeepSeek 冒烟入口；在应用完整调用链复验已记录的真实合成场景；若没有凭证则记录未运行原因，禁止用之前的独立探针冒充应用集成通过。
- [x] 6.3 验证 npm pack 产物可在干净目录安装、headless 启动和离线导出，并记录平台覆盖范围；本阶段不发布到 npm。
- [x] 6.4 运行类型检查、构建、相关测试及 openspec validate --all --strict；交付结果和限制，完成后再归档变更。

## 验收记录

应用验收、真实调用结果、平台范围及限制见 `docs/implementation-validation.md`。当前 apply 已完成；变更保留待审阅，归档作为后续显式操作。

## 7. 审查修复

- [x] 7.1 修复 shell 取消/超时后提前清除进程组强制终止，覆盖忽略 SIGTERM 且重定向输出的后台子进程。
- [x] 7.2 为 shell 双通道及模型响应添加跨块凭证脱敏，验证分块凭证不进入附件、客户端或导出，保留普通二进制输出。
- [x] 7.3 运行类型检查、全量测试和严格规格验证，再次审查修复及相关调用链。
