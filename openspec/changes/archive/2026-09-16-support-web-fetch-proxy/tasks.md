## 1. 代理策略

- [x] 1.1 实现环境/显式覆盖、NO_PROXY 和平台静态代理检测，以 macOS/Windows fixture 验证优先级、禁用、PAC/SOCKS、解析失败和取消。
- [x] 1.2 接入逐跳代理/直连路由、认证隔离与 Journal 证据，以本地 CONNECT 代理验证 Fake-IP、直连固定地址、绕过、重定向和失败不回退。

## 2. 通用网页优化

- [x] 2.1 接入 Deepy 风格请求头和明确验证页分类，验证普通 403/挑战 200/403、响应证据、压缩和摘要回退。

## 3. 验收

- [x] 3.1 验证代理取消、认证材料不进入日志/导出、历史回放不联网；运行完整测试、类型检查、浏览器和安装包验收。
- [x] 3.2 更新 README/开发文档与验证记录，现场验证已启用系统代理访问公开页面，运行 OpenSpec 严格校验并复审更改。

## 4. Review 修复

- [x] 4.1 HTML 实体解码文本在 Markdown 转换前脱敏，合并解析分块；验证标题、代码、表格及特殊字符凭证。
- [x] 4.2 仅对非可信文本字段脱敏，保留 Journal 协议键、状态及 artifact 引用；验证短凭证、引号及失败终态。
- [x] 4.3 使用原始 Content-Type 解码，仅对展示值脱敏；验证凭证与 MIME/charset 重合时仍正确提取中文正文。
