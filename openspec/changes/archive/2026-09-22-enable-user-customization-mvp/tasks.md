## 1. 契约与兼容性基线

- [x] 1.1 定义公开 SDK 版本、资源描述/快照、注册项与能力声明，保留未来 adapter 和 UI 协议版本入口；验证类型检查、未知能力拒绝与旧 Journal 无新字段读取 fixture。
- [x] 1.2 核验并固定 TypeScript 加载器、官方 MCP SDK 及 pi-agent-core 0.85.1 的公开接入方式；用 Node.js 22.19.0/24 完成 TS 扩展加载、两种 MCP transport 初始化及工具注册契约测试，记录版本依据。
- [x] 1.3 建立资源/调用/状态/UI 事件与 artifact 关联，定义字节和资源数上限；用大体积 fixture 验证背压、超限诊断、脱敏及未知事件通用查看。

## 2. 资源目录与管理

- [x] 2.1 实现项目/用户资源发现、清单和内容摘要、来源限定 ID、优先级及重名检测；验证项目覆盖用户、同层冲突、symlink/父路径越界和缺失依赖 fixture。
- [x] 2.2 实现启停、授权范围持久化与撤销、受限用户资源写入；验证未授权项目不执行代码/连接 MCP、已授权范围不重复确认、版本冲突保留原文件。
- [x] 2.3 实现运行资源快照及历史 artifact 保存；验证源文件修改或删除后原运行 revision 与证据仍可读取，无定制项目保持旧行为。
- [x] 2.4 实现候选校验、空闲切换、重载收据和旧资源清理；验证运行中请求无自等待死锁、坏候选保留旧版本、重复重载无重复监听、清理失败进入 degraded。

## 3. 扩展宿主与统一执行

- [x] 3.1 将 CoreTools 的意图/错误记录提升为共用执行包装，保留内置文件修改保护；运行现有 tools/runtime/恢复回归，新增扩展工具的副作用前落盘及截断证据测试。
- [x] 3.2 实现公开 SDK 子路径、TS 工厂加载、命名空间工具/命令注册与短命令消歧；验证不兼容 SDK、重复注册、内置名称冲突和缺失依赖的诊断。
- [x] 3.3 实现命令调度、有限生命周期钩子、后续提示队列及总预算；验证 commandId 重发仅执行一次、前置异常阻止操作、后置异常不改写结果、取消阻止嵌套调度。
- [x] 3.4 实现命名空间状态读写及 schema 版本检查；验证重启恢复、状态不兼容拒绝、工具结果 unknown 不自动重跑。
- [x] 3.5 提供受记录的辅助模型调用服务，复用当前 adapter 并隔离上下文；验证请求字节、用途/扩展关联、逐 attempt 重试、取消及 API key 不泄露。

## 4. Skills、Rules 与上下文

- [x] 4.1 实现受限资源读取接口和安装资料/技能/规则目录授权；验证工作区外已登记资源可读、任意 home 路径和 symlink 越界被拒绝，普通 read 边界不变。
- [x] 4.2 实现 SKILL.md 元数据发现、按需正文和 /skill:<name> 显式调用；验证正文不批量注入、参数正确传入、失效/歧义诊断、skill.loaded 与正文 artifact 一致。
- [x] 4.3 实现用户/根/嵌套 AGENTS.md 解析、目标路径匹配和范围优先级；验证跨目录、多目标、同级隔离和 shell cwd 规则，并说明任意脚本目标解析限制。
- [x] 4.4 实现文件操作前规则变化检查和重新决策；验证首次子目录规则先呈现后执行、连续变化有界失败、被阻止操作无文件副作用。
- [x] 4.5 扩展 prompt manifest/context revision 纳入资源版本、Skill/Rule 正文与作用域；验证禁用撤销指导、稳定排序、当前生效与历史加载分离，运行 context/provider 回归。

## 5. MCP 工具接入

- [x] 5.1 实现配置解析、凭证引用、stdio 和 Streamable HTTP 生命周期；用本地 fixture 验证版本协商、JSON/SSE HTTP 响应、认证失败、stderr 分离和凭证脱敏。
- [x] 5.2 实现分页工具发现、服务器限定命名、schema 映射和列表变化；验证同名隔离、分页完整、无法映射的诊断及当前 run schema 不变。
- [x] 5.3 将 tools/call 接入统一执行包装，保留结构化/多模态/isError/资源引用结果；验证原始证据与模型投影分离、输出边界、链接不自动读取。
- [x] 5.4 实现取消、超时、断线 unknown、重新发现及进程清理；用已执行后断线、忽略终止和列表失效 fixture 验证不自动重发工具、不伪造远端停止。

## 6. 服务协议与 Web

- [x] 6.1 增加资源管理、命令、重载收据及交互回答的版本化协议；验证本地凭据/Host/Origin、单活动运行、commandId 负载冲突与取消隔离。
- [x] 6.2 实现资源管理页及当前/待生效版本、覆盖关系和诊断展示；浏览器验证启停、授权撤销、重载成功/失败和可定位错误。
- [x] 6.3 实现声明式状态/卡片/表单插槽、输入校验和持久化交互；浏览器验证脚本不执行、表单重发只接受一次、断线恢复、超时和取消后旧回答被拒绝。
- [x] 6.4 实现 headless 交互不可用反馈、历史通用渲染和导出/导入兼容；验证删除插件后历史可读且无扩展/MCP 启动，缺失 artifact 明确报错。

## 7. 自定制开发体验与交付

- [x] 7.1 编写随包 SDK 文档、资料索引和工具/命令/钩子/Skill/MCP/UI 示例，补充可信代码边界及 V2 非支持能力说明；验证全部示例通过类型与注册检查，资料版本匹配安装包。
- [x] 7.2 实现 CLI 与宿主的静态校验和显式试运行入口；验证静态检查不执行工厂、试运行宿主状态隔离、语法/依赖/注册错误可修复。
- [x] 7.3 调整发布清单与公共 exports，提供模型可读的开发索引和受限资源管理入口；用干净 tarball 安装验证无需源码仓库即可读取资料、创建扩展并加载。
- [x] 7.4 更新 README、开发指南及 spec.md 的能力状态和两版路线；核对文档不把 V2、强制沙箱或真实模型成功率描述为 V1 已交付。

## 8. V1 综合验收与 V2 进入门槛

- [x] 8.1 用确定性模型和本地 MCP fixture 完成 /review 创建、Skill/Rules 应用、校验、命令执行、卡片/表单、修改重载、重启和停用全链路；验证每步资源 revision、调用证据与真实行为一致。
- [x] 8.2 完成故障矩阵：加载失败、清理失败、持续规则变化、模型重试取消、MCP 已执行后断线、插件删除后导入/导出；验证无未知副作用重放、无重复执行或凭证泄露。
- [x] 8.3 运行 npm run typecheck、npm test、npm run test:browser、npm run test:release、npm run test:pack 及 openspec validate --all --strict；保留结果，失败项修复后才勾选。
- [x] 8.4 在最低支持 Node.js 22.19.0 和 Node.js 24 的干净安装环境验证资料、扩展、MCP 与 Web 闭环，记录 macOS/Linux/Windows 实测或明确未验证限制；安装验收不依赖真实账户。
- [x] 8.5 单独执行并记录真实模型对话自定制演练，包含首次生成失败后的修复和实际使用；记录模型/版本/耗时/问题，不把 fixture 通过视为真实模型验证，不将凭证加入 CI 前提。
- [x] 8.6 对照八份 delta specs 汇总场景与证据，确认 V1 门槛全部满足，列出 V2 接口约束和剩余风险；未完成项留在本 change，V2 只在该验收通过后另建实施 change。

验收与规格映射见 `docs/customization-v1-validation.md`；Linux/Windows 原生环境未实测。V2 不在本次实施范围内。
