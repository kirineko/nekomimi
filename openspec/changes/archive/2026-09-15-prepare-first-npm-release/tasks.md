## 1. 发布输入与包元数据

- [x] 1.1 核查实际 GitHub 仓库、npm 包名/账户权限与许可证选择；记录已确认值及 registry 查询结果，未确认时明确阻止真实发布。
- [x] 1.2 完成 0.1.0 元数据、公开发布配置、LICENSE 和 files 白名单，移除 private；验证 package-lock 一致、npm pack --dry-run 清单及仓库 URL，无个人配置或会话附件。

## 2. 用户文档

- [x] 2.1 完全重写 README，以 npm 全局安装、进入项目目录、nekomimi web、官方密钥获取与 Web 设置为主线；逐条检查可复制命令、更新卸载和错误引导，不要求克隆源码。
- [x] 2.2 将开发与验收说明整理至 docs，准确解释会话分区、用户数据保留、项目文件操作和 shell 权限；验证文档链接且不误宣称系统沙箱。

## 3. 安装与工作区验收

- [x] 3.1 将临时目录和已安装命令调用适配 CI 平台，测试 home/prefix 隔离真实用户目录；验证 Linux/macOS 路径分支和浏览器依赖安装方案。
- [x] 3.2 对待发布 tarball 做临时 prefix 全局安装，从源码目录外启动 nekomimi web；验证无需构建即可加载 Web、配置凭证、提交合成任务和重启持久化。
- [x] 3.3 增加 A/B 工作区及等价路径安装后验收，验证列表不混合、相对写入只修改选定项目、安装目录不变，文件工具拒绝越界且 shell 初始 cwd 正确。

## 4. GitHub CI 与自动发布

- [x] 4.1 新增 push/PR CI，固定 Actions/工具版本并显式安装浏览器；运行类型、测试、构建和安装包验证，确保不读取真实模型凭证，记录尚未远端执行的检查。
- [x] 4.2 新增正式 release.published 发布流程与首发 bootstrap 开关、OIDC 权限及串行控制；验证普通 push/PR/草稿/预发布不发布，标签检出与版本/主线校验逻辑通过。
- [x] 4.3 让最终 tarball 通过安装与内容检查后再上传，增加已存在版本和不确定结果处理；用不上传的演练覆盖版本不符、重复版本、检查失败及权限缺失，验证不盲目回退 token 或重发。

## 5. 交付与首发操作手册

- [x] 5.1 编写首发交互认证发布、registry 安装核查、npm Trusted Publisher 字段配置及后续 GitHub Release 步骤；复核官方流程，区分准备完成、真实首发与首次 OIDC 实测。
- [x] 5.2 运行 typecheck、全量测试、浏览器回归、全局安装 tarball 验收及 openspec validate --all --strict；保存平台与 dry-run 结果，未执行的远端 CI 或真实发布明确标注，不伪造通过。
