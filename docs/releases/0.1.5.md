# Nekomimi 0.1.5

## 变更

- 移除 `web_fetch` 工具、调用指导、服务端 `webFetch` 选项及网页抓取、HTML 提取和代理检测实现。
- 移除 `htmlparser2`、`ipaddr.js`、`undici` 三项直接依赖及无其他引用的传递依赖，共减少 8 个包。
- 保留 `web_search`，旧抓取记录的来源、状态、截断信息与附件仍可查看、导出和导入。
- 构建前清理旧产物，安装包不再携带抓取模块；更新当前使用文档与安装包验收。

## 兼容性

这是一次工具移除：调用方应停止配置 `webFetch` 或调用 `web_fetch`。过时调用按未知工具处理。旧历史及证据不修改，不会自动重新抓取或检测代理。

## 安装

```sh
npm install -g nekomimi@0.1.5 --registry=https://registry.npmjs.org
```

最低 Node.js 版本仍为 22.19.0。

## 验证

复审未发现可操作回归。类型检查、105 项单元/集成测试、13 项浏览器测试及 7 项发布检查通过；OpenSpec 已同步归档，17 项主规格严格校验通过。安装包已通过全局 CLI、Web、配置、工作区隔离、高亮及离线导出导入验收，确认不包含抓取模块。实际发布结果在完成后记录。

## 发布结果

- 发布提交：`4642956db4288bb8d1abac23d7168484a100238e`，标签 `v0.1.5`，均已推送。
- [标签 CI](https://github.com/kirineko/nekomimi/actions/runs/35003661988)：Linux/macOS × Node.js 22.19.0/24.15.0 四组与 Windows 原生安装检查首轮全部通过。
- 五组 CI 通过后创建[正式 Release](https://github.com/kirineko/nekomimi/releases/tag/v0.1.5)，[Publish npm](https://github.com/kirineko/nekomimi/actions/runs/35004283901) 通过 Trusted Publishing 上传成功。
- 官方 registry 版本、keywords、engines、repository 与包一致；下载 tarball 的 SHA-512 与 registry integrity 和来源证明 subject 相同，provenance 指向本次标签、提交及发布工作流。
- integrity：`sha512-YmSS6JAZfFLLnOyxTHQ67ZZptlwtr14ttrBj72SepDwbi/jX0kAbimn319mRfjhJkCPpp6UbdwjY4uRg6uSxDA==`。
- SHA-1：`2b8e8138cea52ffb934c849a0d82cf1bc2e1d6a6`。
- 官方 tarball 在 macOS arm64 / Node.js 22.19.0 上通过本地与全局安装、CLI/Web、配置、工作区隔离、高亮 Worker、离线导出导入验收；包中不存在抓取模块，模型工具集不含 web_fetch。验收目录 `/var/folders/v9/tsjz4byn1mg90zhd5gn680bm0000gn/T/harness-pack-PlsxEG`。
- 发布后 registry 元数据及 tarball 曾短暂返回 404，等待传播并绕过旧缓存后核验，没有重复上传。
- 官方 registry 按包名安装成功，latest 为 0.1.5；`npm audit signatures` 验证 222 个包的 registry 签名与 39 个包的 attestation 全部通过。
