# Nekomimi 0.1.3

## 新增与改进

- 新增独立 `web_fetch`：搜索后读取原网页，也支持直接读取公开 URL；提供可读正文、来源、摘要回退和截断提示。
- 浏览器标签页与页面共用 Nekomimi 猫耳图标，安装包包含图标资源。
- 新增可观测的 DeepSeek Web Search、文件浏览与变更对比，统一工作台右侧面板。
- 新增独立运行诊断，保留 Journal 权威历史和失败证据边界。

## 正确性与边界

- 网页响应证据按字符编码解码、脱敏后保存，成功、失败和取消采用同一路径。
- 修复网页行内代码和表格的 Markdown 保真。
- 抓取仅支持匿名公开 HTTP(S) 文本资源；跨源重定向需要显式读取，不执行网页 JavaScript。支持取消、资源限额和离线历史查看。

## 安装

```sh
npm install -g nekomimi@0.1.3 --registry=https://registry.npmjs.org
```

最低 Node.js 版本为 22.19.0。

## 本地验证

141 项单元/集成测试、11 项浏览器测试、7 项发布元数据测试、类型检查、构建和安装包验收已通过。OpenSpec 主规格已同步，已完成变更已归档。远端 CI 和 npm 发布结果在发布后单独记录。

## 发布结果

- 发布提交：`90312d30235bc1459d723d2870ecb33a616b6506`，标签 `v0.1.3`；代码和标签均已推送。
- [标签 CI](https://github.com/kirineko/nekomimi/actions/runs/34988031314)：Linux/macOS × Node.js 22.19.0/24.15.0 四组及 Windows 原生安装检查全部通过。
- [正式 Release](https://github.com/kirineko/nekomimi/releases/tag/v0.1.3) 在标签 CI 全通过后创建；[Publish npm](https://github.com/kirineko/nekomimi/actions/runs/34988637571) 成功，使用 Trusted Publishing。
- 官方 registry 的版本、keywords、engines 与包元数据一致；下载 tarball 的 SHA-512 与 registry integrity 和来源证明 subject 一致，provenance 指向本次标签、发布工作流和提交。
- integrity：`sha512-hvvWMMLbg9wD2K8T7qtr6W/aFRHY5Kie8a9JbxSQiuv+H5CElm1rIeWTYaD2Yyx737WMqw8bFQ+uN4z7qJbzJA==`。
- SHA-1：`7ca4db35ab3b2bff7bb91f679d9833e6d738c43b`。
- macOS arm64 / Node.js 22.19.0 对官方 tarball 完成临时本地与全局安装、CLI/Web、图标资源、文件配置、工作区隔离、web_fetch 和离线导出导入验收；安装目录 `/var/folders/v9/tsjz4byn1mg90zhd5gn680bm0000gn/T/harness-pack-cvW7nL`。
- npm 上传后索引曾短暂不可见，等待传播后核验，没有重复上传。
- 官方 registry 按包名安装成功；`npm audit signatures --registry=https://registry.npmjs.org` 通过：214 个包的 registry 签名及 38 个包的 attestation 已验证。
